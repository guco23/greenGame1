export class Enemigo {
    maxXp; 
    atk;
    def; 
    prefType; //Tipo preferido para atacar (más posibilidades de que ataque)
    living; //Booleano para la vida, 0 si esta vivo, 1 si no

    act(combatManager) {

    }

    selectTarget(playerTeam) {
        let length = 0;
        
    }
}